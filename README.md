# DevOps Project — Minimal Flask API

## Overview
Tiny Flask API instrumented with Prometheus metrics and OpenTelemetry traces.

## Quickstart (local)
1. Clone:
   ```bash
   git clone <your-repo-url>
   cd devops-project
